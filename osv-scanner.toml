# OSV scanner exceptions for unresolved dev-tooling transitive vulnerabilities.
# These are currently non-runtime for the published plugin and tracked until upstream upgrades land.

[[IgnoredVulns]]
id = "GHSA-f886-m6hf-6m8v"
reason = "brace-expansion 4.0.1 is from wireit in @salesforce/dev-scripts (dev/build tooling only)."

[[IgnoredVulns]]
id = "GHSA-7r86-cg39-jmmj"
reason = "minimatch 9.0.3 is pinned by @typescript-eslint/typescript-estree via Salesforce dev tooling (dev/lint only)."

[[IgnoredVulns]]
id = "GHSA-3ppc-4f35-3m26"
reason = "minimatch 9.0.3 is pinned by @typescript-eslint/typescript-estree via Salesforce dev tooling (dev/lint only)."

[[IgnoredVulns]]
id = "GHSA-23c5-xmqv-rm74"
reason = "minimatch 9.0.3 is pinned by @typescript-eslint/typescript-estree via Salesforce dev tooling (dev/lint only)."

[[IgnoredVulns]]
id = "GHSA-5c6j-r48x-rmvq"
reason = "serialize-javascript 6.0.2 is brought by mocha from @salesforce/dev-scripts (test tooling only)."

[[IgnoredVulns]]
id = "GHSA-qj8w-gfj5-8c6v"
reason = "serialize-javascript 6.0.2 is brought by mocha from @salesforce/dev-scripts (test tooling only)."

[[IgnoredVulns]]
id = "GHSA-h67p-54hq-rp68"
reason = "js-yaml 3.14.2 quadratic-complexity DoS in YAML merge-key handling (MEDIUM). Transitive via gray-matter (md-to-pdf). The v4.2.0 fix removes yaml.safeLoad, which gray-matter calls at runtime, breaking PDF doc generation. No patched 3.x release exists. Used only to parse our own markdown frontmatter, not untrusted input."
