# Generated by the commands under test, never part of the sources.
# Without this the scenarios commit them with `git add -A`, and switching branch then fails
# because the next command has already rewritten them.
hardis-report/
.sf/
.sfdx/
config/user/
